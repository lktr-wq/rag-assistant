# AI 协作日志

## 记录格式

每条记录包含五字段：目的、输入、建议、人工判断、验证

---

## [2026-06-22] S3: 编写 Spec

**目的**：请 AI 帮助理解 RAG 项目需求，编写 spec.md

**输入**：course-faq.md 全文、README.md 项目说明

**建议**：
- AI 建议添加用户登录功能
- AI 建议使用向量数据库存储
- AI 建议支持 PDF 上传

**人工判断**：
- 拒绝用户登录——课程要求 CLI 工具，单用户本地运行
- 拒绝向量数据库——课程约束不引入外部存储
- 拒绝 PDF 上传——仅支持 Markdown 格式

**验证**：spec.md 包含 5 个目标、5 个非目标、5 个验收标准

---

## [2026-06-22] S5: 定义接口契约

**目的**：请 AI 帮助定义 retrieve() 和 answer() 函数签名

**输入**：test_basic.py 测试要求

**建议**：
- AI 建议 retrieve() 返回嵌套字典结构
- AI 建议 answer() 返回包含 token 数的字典

**人工判断**：
- 采用简单列表返回——测试要求 retrieve() 返回 list
- 采用简单字典返回——测试要求 answer() 返回 dict 含 answer 和 sources

**验证**：test_basic.py 接口契约检查通过

---

## [2026-06-22] S5-2: 修复接口契约

**目的**：test_basic.py 测试失败，retrieve() 和 answer() 抛出 NotImplementedError

**输入**：测试错误信息

**建议**：
- AI 建议返回 mock 数据
- AI 建议跳过测试

**人工判断**：
- 采用返回空值——retrieve() 返回 []，answer() 返回 {"answer": "", "sources": []}
- 拒绝跳过测试——必须通过全部测试

**验证**：test_basic.py 9/9 通过

---

## [2026-06-22] S6: 设计 RAG 数据流

**目的**：请 AI 帮助设计检索策略和 Prompt 模板

**输入**：course-faq.md 格式、test_basic.py 要求

**建议**：
- AI 建议使用 embedding 向量检索
- AI 建议使用 TF-IDF 算法
- AI 建议 Prompt 中要求模型输出来源

**人工判断**：
- 拒绝 embedding——课程约束不引入外部依赖
- 拒绝 TF-IDF——10 条 FAQ 规模小，关键词匹配足够
- 采用 Prompt 要求输出来源——简化来源提取逻辑

**验证**：Mock 服务验证成功，返回正确来源

---

## [2026-06-22] S7: 实现检索模块

**目的**：请 AI 帮助实现 parse_faq()、tokenize()、retrieve()

**输入**：design.md 检索策略、course-faq.md 格式

**建议**：
- AI 建议使用 jieba 分词
- AI 建议使用单字匹配
- AI 建议阈值设为 0.5

**人工判断**：
- 拒绝 jieba——不引入外部依赖
- 拒绝单字匹配——中文 2-gram 更能捕捉词级语义
- 采用阈值 0.15——太低会误答，太高会拒答正确问题

**验证**：测试通过，正确匹配和拒答都正常

---

## [2026-06-22] S7-2: 配置 MiMo API

**目的**：请 AI 帮助配置 MiMo-V2.5-Pro API 替代 Mock 服务

**输入**：MiMo API key、API base URL

**建议**：
- AI 建议硬编码 API key
- AI 建议使用配置文件

**人工判断**：
- 拒绝硬编码——API key 应该用环境变量
- 采用环境变量——LLM_API_KEY、LLM_API_BASE、LLM_MODEL

**验证**：MiMo API 调用成功，返回正确回答

---

## [2026-06-22] S7-3: 修复来源重复显示

**目的**：模型回答中输出来源，main.py 又打印一次来源，导致重复

**输入**：测试输出截图

**建议**：
- AI 建议修改 Prompt 要求模型不输出来源
- AI 建议删除 main.py 的来源打印

**人工判断**：
- 采用从回答中提取来源——模型可能不遵循 Prompt 指令
- 删除回答中的来源部分——避免重复显示

**验证**：测试通过，来源只显示一次

---

## [2026-06-22] S8: 工程交付证据链完备

**目的**：完善交付所需的 6 类证据文件

**输入**：spec.md, design.md, ailog.md, testrecord.md, README.交付版.md, reflection.md

**建议**：
- AI 建议只写新文件，已存在的文件不用管
- AI 建议把所有文件放在根目录
- AI 建议 ailog.md 记录所有 AI 建议（包括被拒绝的）

**人工判断**：
- 采用检查已存在的文件——spec.md 和 design.md 需要检查是否完整
- 采用放在 docs/ 目录——符合项目结构规范
- 采用记录被拒绝的建议——体现人工判断的价值

**验证**：6 个证据文件齐全，test_basic.py 9/9 通过

---

## [2026-06-22] S8-2: 创建 Skill

**目的**：总结项目经验为 Skill，用于指导后续开发

**输入**：项目反思、踩坑记录

**建议**：
- AI 建议创建在 ~/.codex/skills/ 目录
- AI 建议只写技术细节
- AI 建议用英文编写

**人工判断**：
- 采用创建在项目目录——便于项目内引用
- 采用添加核心理念——螺旋上升与全程溯源
- 采用中文编写——项目本身是中文的

**验证**：Skill 创建成功，包含核心理念和最佳实践

---

## [2026-06-22] S9: 测试验证与流程图解

**目的**：运行完整测试套件，添加流程图解

**输入**：test_basic.py、项目备份历史

**建议**：
- AI 建议只运行测试，不需要流程图
- AI 建议流程图用 Mermaid 语法
- AI 建议流程图用垂直布局

**人工判断**：
- 采用添加流程图——复杂地方需要图解说明
- 采用 ASCII 图——兼容性更好，不需要渲染
- 采用蛇形布局——更清晰，一个框的下一个框在右侧

**验证**：测试 9/9 通过，流程图包含 Git 流程、函数执行、检索策略、拒答逻辑、目录结构
